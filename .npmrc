# npm is used only as a script runner (npm run dev/build/check).
# Package installation is handled by Bun (bun install).
# Disable npm's allow-scripts check — it's irrelevant when npm isn't
# the installer, and the warnings about esbuild/sharp/workerd install
# scripts are noise since Bun already handles those correctly.
allow-scripts=false
