---
import LegalLayout from '../../../layouts/LegalLayout.astro';
import { getContent } from '../../../lib/i18n';
import { seoPaths } from '../../../lib/seo/paths';

const locale = 'sk' as const;
const { terms } = getContent(locale).legal;
---

<LegalLayout content={terms} paths={seoPaths.terms} locale={locale} />
